from django.urls import path



urlpatterns = [
    # path('lists/', BlogListAPIView.as_view(), name='list'),
    # path('add/', BlogAddAPIView.as_view(), name='add'),
    # path('<str:slug>/', BlogRetrieveAPIView.as_view(), name='details'),
    # path('<str:slug>/edit/', BlogEditAPIView.as_view(), name='edit'),
    # path('<str:slug>/delete/', BlogDeleteAPIView.as_view(), name='delete'),
    # path('<str:slug/check/', BlogCheckAPIView.as_view(), name='check'),
]
